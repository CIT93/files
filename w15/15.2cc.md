# Week 15.2: Start Fetch Code Challenge


* Code Challenge 15.2 - Fetch API 
    - `aqi-data-service.js`
        - Steps
            - // Write the fetchAqiData function and add these comment above the function

            - // Fetches the AQI data from an external API based on latitude and longitude.
            - // @param {number} lat - Latitude.
            - // @param {number} lon - Longitude.
            - // @returns {Promise<Object>} A Promise that resolves with the parsed JSON data.

            - // Refer to the open weather documentation https://openweathermap.org/api/air-pollution and review the how to make the API call

            - // declare variable `url` and determine it's value based on the documentation 
            - // declare a variable `response` and fetch the `url` and await the response
            - // we will code the error later, but you can attempt it if you want

            -  // declare variable `data` wait the json conversation
            -  // return data
                - * Commit your changes with the message: **"my attempt at fetchAqiData function"**
    - `aqi-integrator.js`
        - Steps
            - // update the import statement to include the fetchAqiData function
            - // declare a variable `aqiData` and determine where to call the fetchAqiData and pass the latitude and longitude as arguments and await its result.
            - // console log aqiData
            - // Update the code challenge with a ## 15.2 Code Challenge Comment and tell me if you were successful to and copy the console output if you were into the file and if not tell me what you tried. 
                - * Commit your changes with the message: **"my attempt at calling fetchAqiData function"**

        * **Git Workflow (Save Progress):**
            * Commit your commit history for the w15 folder
